// Generated by CoffeeScript 1.10.0
define(['can', 'can/control', 'homeCtrl', 'Auth', 'localStorage', '_'], function(can, Control, homeCtrl, Auth, localStorage) {
  return Control.extend({
    init: function(el, data) {
      var menus;
      menus = localStorage.get('menus');
      if (can.home == null) {
        can.home = new homeCtrl('body', {});
      }
      return can.home.showFirstMenu();
    }
  });
});
